# Verdict: polars output + 0.1.0 release readiness

- Date: 2026-06-26
- Branch: `main`
- Backend: cpu
- Artifacts: `dist/catstat-0.1.0{.tar.gz,-py3-none-any.whl}` (build output, gitignored),
  `CHANGELOG.md`, `docs/publishing_checklist.md`
- Roadmap target: Phase 3 (polars) + release prep

## Question
Add `output="polars"` and confirm `catstat` is **release-ready at 0.1.0** — does it build, pass
`twine check`, and install + import cleanly from the wheel?

## Evidence
- **polars**: `output="polars"` returns a `pl.DataFrame` with the right columns/dtypes (incl.
  multiclass naming); lazy import, optional dependency (`polars` extra). `test_polars.py`
  (skips if polars absent). `bash scripts/check.sh`: **88 passed, 2 skipped**.
- **Version**: bumped 0.0.1 → **0.1.0**, synced across `pyproject.toml` and `src/catstat/__init__.py`.
- **Build**: `python -m build` produces sdist + wheel; `twine check` **PASSED** on both. Wheel
  bundles all modules **and `py.typed`**.
- **Clean-venv install**: `pip install` the wheel in a fresh venv → `import catstat` (0.1.0) and a
  full `fit_transform` / `scheme="ordered"` / custom-`q90` run succeed on **sklearn 1.9.0** (latest)
  — confirms forward-compat with current scikit-learn (the dev box has 1.2).
- Added `LICENSE` (MIT), `CHANGELOG.md`, `docs/publishing_checklist.md`, and the `release-prep`
  skill.

## Decision
**KEEP / SHIP-READY** — 0.1.0 builds, checks, and installs. The remaining steps (`twine upload`,
`git tag v0.1.0`, GitHub release) require maintainer credentials and are intentionally left to the
maintainer per `docs/publishing_checklist.md`.

## Follow-ups
- Optional before/after upload: API docs (pdoc, `docs` extra), estimator-check hardening, and the
  GPU on-device perf redesign (KI-020) to eventually re-enable `backend="auto"` GPU.
